# Python SLN Parser

SLN is the Scopes List Notation.

## Developing

Uses `hatch` for the build system so install that.

```sh
  make bumpversion
  make build
  make publish
  # enter credentials
```
